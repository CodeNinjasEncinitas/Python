while True:
    length = input("Enter a Length: ") #
    length = int(length) #
    width = input("Enter a Width: ") #
    width = int(width) #
    area = length * width #
    print(f"Area = Length x Width = {length} x {width} = {area}") #
    print("----------------------------------------------") #
